const sections = [["Profile", "Your safe account identity is managed by the authentication service.", "Available soon"], ["Security", "Password and active-session controls will be added here.", "Coming later"], ["Organizations", "Team and organization management is not implemented yet.", "Coming later"], ["Billing", "No billing or subscription behavior is active.", "Coming later"]];
export default function SettingsPage() { return <div className="page-stack"><header className="page-heading"><div><span className="eyebrow">Account</span><h1>Settings</h1><p>Manage your future Zayloq account and workspace preferences.</p></div></header><div className="settings-list">{sections.map(([title, description, status]) => <section className="surface-card" key={title}><div><h2>{title}</h2><p>{description}</p></div><span>{status}</span></section>)}</div></div>; }
